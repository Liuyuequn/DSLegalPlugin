# AGENTS.md

> 本文件是 AI 代理（及人类协作者）进入本项目的入口说明。修改代码或文档前请先读完本文件。

---

## 1. 项目是什么

**DSLegalPlugin** —— 运行在 DeepSeek Harness（DSH）平台上的律师**日程与待办可视化管理工作台**。

- 管理两类**并列**事项：**日程**（有具体时间）、**待办**（无具体时间）。
- 覆盖 8 类法律服务，**逐项实现**；**首批实现民事诉讼**，其余 7 类暂不推进。
- 数据以本地文件（markdown）承载，**文件即数据库**，不引入传统数据库。
- 个人本地使用，装入个人 DSH 的 `web` profile。

---

## 2. 文档索引

所有非代码类文档都在 `maintenance/`：

- `maintenance/1. DSLegalPlugin 需求分析与技术栈设计.md` —— 需求范围、技术决策、系统架构、开发路线图。
- `maintenance/2. DSLegalPlugin 数据约定规范.md` —— 数据格式契约（**用户 / AI / 插件三方共用**）。
- `maintenance/3. DSLegalPlugin 事故复盘与排障手册.md` —— 事故复盘、**按症状索引的排障手册**、平台陷阱清单、交付前验证清单。
- `maintenance/4. DSLegalPlugin 技术分析评价报告.md` —— 2026-09-10 全量代码评审（第二版）：**以风险为轴、以不变量为纲**的五轴评价体系（数据不变量完备性 / 故障模式设计 / 三方契约一致性 / 平台边界健康度 / 复杂度体量相称性）、12 条数据不变量核对表、风险与债务清单、改进建议排序。

**阅读顺序**：先读《需求分析与技术栈设计》第 1–3 节了解定位与技术栈，再读《数据约定规范》了解数据契约；**出问题时先查《事故复盘与排障手册》第 2 节**；**规划下一步开发时先读《技术分析评价报告》第 8–9 节（风险清单与建议排序）**。

---

## 3. 文件组织约定

- **非代码类、人与 AI 沟通的文档** → `maintenance/`
- **代码类文档** → 对应代码包内（如 `packages/core/README.md`）
- **代码** → `packages/` 下的三个包

```
DSLegalPlugin/
├── AGENTS.md          # 本文件
├── maintenance/       # 非代码类文档
└── packages/
    ├── core/          # 领域核心：数据模型、文件约定与读写、查询/分组/排序（纯逻辑，可独立发布）
    ├── host/          # DSH host 插件：工具注册、文件访问、与 DSH 服务集成
    └── client/        # DSH client UI：日历视图、待办视图、项目分组
```

---

## 4. 技术栈

- **语言 / 运行时**：TypeScript（strict）+ Node.js ESM
- **包管理**：pnpm workspace（monorepo）
- **构建 / 打包**：tsdown（build / watch）
- **测试**：vitest
- **Host 插件框架**：Cordis（`{name, inject, apply}`）
- **插件配置校验**：schemastery（Cordis 约定）
- **工具 schema**：`@deepseek-ai/dsh-tools` 的 `defineTool` 声明式 DSL
- **数据承载**：Markdown（`##` 章节 + 方括号字段），自建解析器（`@dslegal/core`）；不引入 YAML / frontmatter / remark
- **日期时间**：date-fns
- **农历 / 节气 / 法定节假日**：`lunar-javascript`（MIT，core 的唯一运行时依赖；见 8.2「月历的农历与法定节假日」）
- **文件监听**：chokidar
- **Client UI**：React 18 + `dsh-client-ui-primitives` + slots
- **Client 状态**：zustand
- **样式**：DSH 主题 CSS 变量 + clsx（不引 Tailwind）

---

## 5. 关键约束（违反会导致功能失效或返工）

### 5.1 DSH 平台

1. **插件导出格式**：必须是 `{name, inject, apply}` 的命名导出；**禁止 `export default`**（会被 loader 拆包导致 `inject` 丢失，插件静默失效）。
2. **patch 层语义**：`cordis.patch.yml` 中一行是 `{id, name, config}`；覆盖是**整行 config 替换**，不是深合并。
3. **工具命名**：一律加 `legal_` 前缀（如 `legal_todo_add`），避免与 DSH 内建 `todo_write`（agent 自身任务清单）和 `schedule_create` / `schedule_list` / `schedule_delete`（DSH 提醒系统）语义冲突。
4. **schema 前缀稳定**：工具 schema 一旦发布尽量不改，否则废掉 KV cache 复用。
5. **接口以实查为准**：平台为 rc 版本，所有 Service / Slot / Event 以 `cordis_inspect_*` 查询到的真实签名为准，不要凭文档或记忆猜 API。
6. **不依赖 DSH 的传递依赖**：插件装进 profile 后有自己的 node_modules，须自行声明依赖。平台服务优先用**结构化契约**声明（如 `http.ts` 的 `WebServerLike`、`settings.ts` 的 `SettingsProviderLike`），不 import 平台包：既能少一个依赖，也避免 pnpm 链接变体缺失导致 TS 找不到类型。
7. **用户设置走 `ctx.settings`**（`$DSH_HOME/settings.yaml`，热重载），不要另建配置文件——DSH 自己的设置界面也在读写同一份文档，两处真相必然打架。接线方式：组合层配置作为 `base`、用户层覆盖；`settings` 服务缺席时退回组合层（见 `packages/host/src/settings.ts` 的 `installSettings`）。

### 5.2 数据读写（本项目特有）

7. **写回必须"外科手术式"**：只替换被编辑条目对应的那一行，文件其余部分**字节不变**；不得整篇重新序列化（会破坏用户手写排版）。
8. **文件是唯一真相**：界面编辑与外部编辑冲突时**以文件为准**，界面提示"文件已变更，已重新加载"。
9. **文件监听要抑制回环**：自身写入时打标记，避免"写文件 → 触发监听 → 又刷新"的循环；并对短时间多次变更做防抖。
10. **插件不创建目录结构**：数据根目录与目录树由用户自行构建；插件只读写 `1. 工作日志.md` 的两个章节。
11. **不擅自修复**：章节缺失或条目格式异常时，保留原样 + 界面提示；不静默丢弃、不自动重建。

---

## 6. 数据契约（摘要）

完整定义见《数据约定规范》。要点：

- **目录**：`<数据根目录>/<顶级目录>/<项目>/0. 协作/1. 工作日志.md`
- **顶级目录名可配置**（映射到 8 类服务）；`0. 协作`、`1. 工作日志.md` 名称**固定不可配置**。
- **两个二级章节**：`## 1. 待办事项`、`## 2. 日程安排`。
- **H1 标题即类别**：`# 工作日志_<类别>`（诉讼类为 `_民事` / `_刑事` / `_行政`），是判定项目服务类别的**唯一依据**；缺失或无法识别时类别记为未知，章节仍可读写。
- **条目格式**：
  - 待办：`- [ ] [标题]，[优先级]，[备注]`
  - 日程：`- [ ] [标题]，[优先级]，[备注]，[开始日期]，[结束日期]，[开始时间]，[结束时间]，[地点]`
- **字段由半角方括号 `[ ]` 界定**；全角逗号仅作书写分隔，**可自由出现在字段内容中**。
- 空字段写 `[]`；字段内容中不得出现半角方括号（改用全角 `【】`）。
- **优先级四类与颜色**：重要且紧急（赤 #E53E3E）/ 紧急不重要（橙 #ED8936）/ 重要不紧急（黄 #f3e417）/ 不重要不紧急（绿 #98c51e）。这四个是**默认显示色**，用户可在「插件设置」里改（存 `settings.yaml`，不是数据）——**文件里只写优先级文字，永远不写颜色**。
- **时间语义**：开始日期必填；只有开始日期 = 全天事项（当日全天"进行中"）；有开始时间无结束时间 = 持续到当日结束。
- **时间区间校验**：结束时刻不得早于开始时刻，否则标记格式异常并明确提示错误。
- **"进行中"是派生状态**（按有效时间区间与当前时刻计算），**不写入文件**。

---

## 7. 开发工作流

本机未全局安装 pnpm，统一通过 **corepack** 调用（Node 24 自带 corepack）：

- 安装依赖：`corepack pnpm install`
- 构建全部包：`corepack pnpm build`
- 测试全部包：`corepack pnpm test`
- 类型检查：`corepack pnpm typecheck`
- 单包操作：`corepack pnpm --filter @dslegal/core test`
- 装入 DSH profile：`dsh plugin --profile web add <包名或路径>`
- 快速原型：使用 DSH 运行时动态插件（`cordis_define` / `cordis_run`），不打包即可验证

依赖版本（已锁定）：TypeScript 7、vitest 5、tsdown 0.23、Node ≥ 20。

---

## 8. 当前状态与关键决策

**状态**：**P1–P7 已完成**——core 领域引擎 + host（10 个 `legal_*` 工具 / 扫描 / H1 类别 / 原子读写 / chokidar 监听 / `/dslegal/*` HTTP）+ client（`shell.overlay` 的「法程」面板，以三条线索组织）；**已装入 `web` profile**；共 **313 项测试**（core 106 + client 159 + host 48，含插件装配与 HTTP 端到端集成测试），typecheck、build 全通过。

**P7 之后的九轮修订**（2026-09-12）：①**四象限颜色可由用户在「插件设置」里自定义**；②**月历格子铺上农历 / 节气 / 中国传统节日 / 法定节假日「休 · 班」**，同时把月历格线从"每格一圈深框"改成**极浅的内部细分隔线**；③**月视图的点击粒度从"日"细化到"日程"**——点某一条日程弹它的明细悬浮窗，不跳日视图（那时"点空白"还什么都不做，**第 ⑦ 轮把它改成了"点空白 → 新建"**，但"不跳日视图"照旧）；④**「法程」启动按钮可拖动**，并修掉"侧边栏折叠后被面板盖住"；⑤**明细悬浮窗里加了切换完成状态的按钮，并复用到周视图**（周形态原先"点整条直接切状态"的手势随之取消）；⑥**项目详情改版**——待办按优先级铺成 **2×2 四象限网格**，并修掉"日程安排浮在界面上、盖住「不重要不紧急」那一组待办"的压扁 bug；⑦**点空白处新建待办 / 日程**——点各容器的空白（含标题行）弹出新建悬浮窗，"点在哪儿"决定预填的优先级与日期；⑧**新建窗的项目选择器**——项目不再写死，可搜索、可切换；⑨**使用说明改版**——默认全部折叠、点标题展开，按用户的动手顺序重排。详见 8.2 各小节。

**P7 = 界面视觉重构**（2026-09-11）：按三张参考图（日 / 周 / 月各一张）重做三种形态，并把同一套语言推到待办 / 项目 / 使用说明。**参考图与逐图实测记录已于 2026-09-12 从仓库和磁盘上一并删除**——图上是用户真实的日程与案件名，而本仓库是公开的。所以 **8.2 的两张表就是取值的唯一事实来源**：**不要凭记忆改配色**，要改就先去界面里量。

**界面信息架构（P6 重构，2026-09-10）**：三条**并列且互斥**的线索，同一时刻只呈现一条，切换线索不改动数据。

- **日程**：日 / 周 / 月三种形态，各自展示对应范围内的**全部**日程安排；一条日程"属于哪天"由**有效时间区间**决定（跨日事项在覆盖的每一天都出现，跨月同理）。顶部第二行是**三区网格**（`scheduleHead`，`1fr auto 1fr`）：左边切形态（日 / 周 / 月），**中间恒显当前聚焦的那一天**（就是 `cursor` 本身，`YYYY 年 MM 月 DD 日`，三种形态写法一致），右侧是条数统计，**最右端固定「今天」**。用网格而非 flex 是因为 flex 的"居中"要靠两侧等宽，而左侧三个按钮与右侧统计天然不等宽，日期会被推偏。**中间那一格不换算成"区间起点"**——换算了以后翻页会让它的含义跟着变（月视图翻一次就成 1 号），用户就没法靠它定位自己；配套地 `shiftMonth` 也**保留日号**（09-12 → 08-12），日号超出目标月天数时夹到月末。
- **待办**：四个优先级各占一个**等面积象限**（2×2 铺满可视区）；每格只显示**最近创建且当前界面放得下**的条目，**只有标题与完成状态**——不显示备注，**也不缀项目标签**（行高固定 26px、格子才 550px 宽，再塞一个案件名会把标题挤成省略号；所属项目走悬浮提示），标题过长省略；排序为**未完成在前、已完成一律排在所有未完成之后**，两组内部都按行号倒序（行号 = 创建时间近似，见《数据约定规范》5.1.1）；未设优先级的待办不进象限，只报条数。
- **项目**：先列全部已就绪项目，点进某个项目看它**全部**待办（按优先级铺成 **2×2 四象限**、**带备注**）与日程（按时间升序）。
- **使用说明**：首行菜单上带文字的「使用说明」入口（盖在面板上的说明界面，不是第四条线索），首次打开自动展示一次（`localStorage` 标记）。说明文案是 `HELP_SECTIONS` 里的**字符串数据**，重点用 `**这样**` 标；`views.tsx` 的 `richLine()` 在渲染时把成对 `**` 换成 `<strong>`（**只认这一种标记**，不引 markdown 解析器）。实测踩过：先写了 `**` 文案却没写 `richLine`，界面上就原样显示 `**择一**`。**防复发守卫**：`packages/client/test/help.test.ts`（扫源码，检查 `**` 成对 + 说明行确实走 `richLine`）。
  **2026-09-12 改版：十几节正文一次铺满 → 默认全部折叠、点标题展开。** 用户原话："不要一股脑堆在一起……其他内容默认折叠，用户点击问题标题后再展开……把重要的介绍放在前面，比如「数据放在哪」……「五张日卡叠成一扇」这些事用户一看就知道了……「怎么打开这个工作台」按道理要放在前面。"四条规矩：① **开场说明恒定可见**（"最基础的介绍"留在折叠列表之外）；② **顺序按用户的实际动作排**：怎么打开 → **数据放在哪**（本项目最该先讲、也最难自己猜出来的一条）→ 三条线索 → 怎么新增 → 各线索细节 → 颜色 → 插件设置（压尾）；③ **删掉"一看就知道"的描述**，并把「在哪里可以勾选」并进「三条线索」、「数据安全边界」并进「数据放在哪」；④ 每条带 `hint`（折叠时它就是索引，**靠右**显示），**可同时展开多条**（不做互斥手风琴——用户常要对着两条看）。守卫：`layout.test.ts` 的「使用说明：折叠式与排序」（默认折叠、顺序、hint 齐全、开场说明在折叠列表之外、禁回"五张日卡"这类废话）。探针 `probe-help.mjs` 分四段（默认折叠 / 顺序 / 展开收起 / 展开全部后再查正文）——**正文检查必须放在展开之后**，折叠时正文根本不在 DOM 里。
- **首行菜单**（面板顶端那一条）从左到右只有四组：线索切换（日程 / 待办 / 项目）、「使用说明」、「插件设置」（进去改数据根目录）、「数据刷新」。**不放插件名**——面板已占满会话区，左上角再写一遍「法程」只是重复占位（启动按钮仍叫「法程」，那是侧边栏上的浮标）。右侧三个入口**共用同一个 `textButton` 样式**：早先只把「使用说明」画成描边主色按钮，反而让人以为另外两个不可点。**插件设置子页的顶端只留一个「返回」**——那是个"改完就走"的表单，右侧堆两个按钮只会让人犹豫点哪个；使用说明在首行菜单上一直有。
- 数据一次取回：`/dslegal/overview` 返回完整数据集（全部项目 + 全部待办含已完成 + 全部日程），三线索在本地切片，切线索不发请求、不闪。

### 8.2 视觉语言（P7）

**参考图实测**出的共同语言，改样式前先读这一段：

| 用途 | 取值 | 来源 |
| --- | --- | --- |
| 页面底 | `#FAFAFB`（实现为 `rgba(0,0,0,.022)` 叠在主题底色上） | 三张图共有 |
| 卡片 / 格子 | `#FFFFFF` | 三张图共有 |
| 凹槽 / 周泳道 | `#F1F1F2`（实现为 `rgba(0,0,0,.055)`） | P7 参考图实测，与实现像素级一致 |
| 主色 | `#4A8CF1` | P7 参考图实测（**不是** `#2196F3`） |
| 次强调 | `#7E7BEA` | 同上 |
| 发丝线 | 10% 黑（`--dsw-alias-border-l2` → 实测渲染 `#e6e6e6`） | P7 参考图的 `#E8E8E8` = 9% 黑 |
| 格线（更浅一档） | 4% 黑（`--dsw-alias-border-l1` → 实测渲染 `#f5f5f5`） | 用户要求"更细更浅的灰色线条划分日期"，见下 |
| 法定节假日「休」 | `#D64545`（`T.rest`） | 刻意不复用任何优先级色 |

> 实测提醒：`--dsw-alias-border-l1/l2` 在 `document.documentElement` 上**读出来是空串**（DSH 的主题变量不在根元素上），所以插件里这两个 `var()` 实际上一直在走**兜底值**。查这两个 token 的值不要靠 `getComputedStyle(root)`，要直接量元素渲染出来的颜色。

三条必须守住的规则：

1. **层次靠明度差，不靠描边堆叠**。页面底、卡片、凹槽三档中性色是整套界面的骨架；主色只出现在"今天"、实心条与浮标上，占屏不到 4%。一旦把主色铺开（通栏色带、彩色表头），界面立刻退回"廉价后台"。
2. **条的颜色 = 优先级**，`#f3e417`（重要不紧急）与 `#98c51e`（不重要不紧急）亮度太高，**必须走 `onColor()` 按对比度择优取深色字**；一律白字会让对比度掉到 1.3:1。未设优先级的条目用中性石板灰 `T.unset`，**不能拿主色兜底**——主色在这套界面里专表"今天"。
3. **已完成 = 划线 + 置灰**（用户明确要求），且一律排在所有未完成之后；不靠降透明度（会把标题一起淡化到读不清）。

**字号**：一律取自 **DSH 自己的字体刻度**（`styles.ts` 的 `FS`，映射到 `--dsw-font-*` 变量），不再自造 11/13/15/19 这类数字——宿主将来调刻度，插件跟着走。全插件只用四档，且**以 16px 为默认**（`panel()` 的 `fontSize: FS.base`，等于 `--dsw-font-base-16`，也就是 DSH 正文那一档，实测 `document.body` 就是 16px）：

| 档 | 值 | DSH 变量 | 用途 |
| --- | --- | --- | --- |
| 大 | 20px | `--dsw-font-l-20` | 周历日号 |
| 默认 | 16px | `--dsw-font-base-16` | 正文、条目标题、面板标题、焦点日期 |
| 次要 | 14px | `--dsw-font-s-14` | 元信息、提示、导航与按钮文字 |
| 微 | 12px | `--dsw-font-xxs-12` | 标签、条内文字、月历小条、计数药丸 |

字号变大后固定高度要跟着调（正文 13→16、行高 24 之后，26px 的待办行只剩 2px 余量）：`TODO_ROW_HEIGHT` 26→30、`MONTH_CHIP_HEIGHT` 20→22。

**三种形态的造型**：日 = 五张**重叠日卡**（当天居中、z 最高、满不透明，两侧依次下沉变淡，步距 62px，点邻居卡即居中）；周 = 七列**斑马泳道**（白 / `#F1F1F2` 交替，不画竖线，今天列顶端一条主色内阴影）+ **实心色条**（高 30px、圆角 6px、右端带 `HH:MM`）；月 = `rows×7` **格线格**（行数按真实周数取 5/6，不照 6 行铺）+ 格内小条（高 22px）+ 放不下时写「+N」。

**月历的格线（2026-09-12 改版）**：只有**内部分隔线**，外面不描边。用户原话是"当前月份实际包含的日期全部用黑色边框标注起来了，很丑，这没有必要，月视角全局都改用更细更浅的灰色线条划分日期即可"。改版前有两层问题叠在一起，实测像素证据（横跨竖缝取 7 个像素）：

| | 缝上的像素 | 观感 |
| --- | --- | --- |
| 改版前 | `#ffffff #ffffff #f4f4f4 #a8a8a8 #a8a8a8 #ffffff #ffffff` | 1px 浅灰 + **2px 中灰**，格子像被描了黑框 |
| 改版后 | `#ffffff #ffffff #f5f5f5 #ffffff #ffffff #ffffff #ffffff` | 恰好 1px `#f5f5f5` |

- **主因是 `<button>` 的 UA 边框**（`border: 2px outset` → 渲染成 2px 的 `#a8a8a8`），不是"发丝线叠成 2px"。只写右、下两条边时，上、左就留下 UA 那 2px。**四条边一律显式写全**，别依赖简写与 longhand 的顺序。
- 次因是外层画四边、格子又画上/左，接缝叠成 2px。现在**外层 `border: 'none'`、格子只画右/下，最后一列 / 最后一行不画**——每条缝只画一遍。
- **不再整格降透明度**表示"本月之外"：那会把格线一起淡化，反而让本月的格线相对突出成一圈边框。"本月之外"改由**文字颜色**（日号 `labelPrimary → labelCaption`、字重 600→500）与**小条区 `opacity: 0.55`** 表达。
- 格线颜色用新令牌 `T.hairline`（`--dsw-alias-border-l1`，4% 黑，实测 `#f5f5f5`），比原来的 `T.border1`（10% 黑，`#e6e6e6`）再浅一档。
- **防复发守卫**：`packages/client/test/layout.test.ts` 里两组——「月形态：格线只画一遍，且不整格降透明度」是针对月历的具体断言；「用在 `<button>` 上的样式必须把边框说全」是通用防线，它**从 `views.tsx` / `client.tsx` 里现扫 `<button ... style={UI.x}>` 得到名单**（新按钮自动纳入，不用手工维护），再逐条检查。注意断言写的是 **`'none'` 而不是 `undefined`**：`undefined` 的含义是"交给 UA 决定"，正是它让第一版守卫在 bug 存在时照样通过。

**月历的农历与法定节假日（2026-09-12 新增）**：格子首行固定三格 `[阿拉伯数字日号] [休/班] ……… [农历/节日]`。

- **右上角那一条三选一、互斥**，按「中国传统节日 → 二十四节气 → 农历初一显示**月份**名 → 其余显示农历**日期**」择一。这就是用户的原话规则。
- **「休 / 班」与那一条不是互斥关系，可以同时出现**，位置夹在日号与农历之间（用户指定）。`休` = 法定节假日放假，`班` = 调休上班（本来是周末却要上班）。`班` 是顺手加的——不标出来，一个"要上班的周日"会被读成休息日。
- 分工：`packages/core/src/almanac.ts` 提供 `almanacFor(key)` / `almanacLabel()` / `holidayMark()` / `holidayMark` 与 `hasHolidayData()`；`monthDayAlmanac` / `monthDayMark` / `monthDayNum` 负责样式。`almanacFor` 带**模块级缓存**（月视图一次算 35–42 天，而 hover / 光标移动都会整块重渲染）。
- **公历节日（元旦 / 国庆节 / 教师节…）刻意不进副标题**——用户要的是"中国传统节日节气"。实测 `lunar-javascript` 的**农历** `getFestivals()` 全量就是 9 个纯传统节日（除夕 / 春节 / 元宵节 / 龙头节 / 端午节 / 七夕节 / 中秋节 / 重阳节 / 腊八节），没有现代节日混进来，所以不需要白名单；2026 年恰好 24 个节气日，且节气与节日从不撞天。这些都写成了断言（扫一整年逐日核对）。
- **法定节假日不可推算**：它是国务院逐年公布的安排。上游 `HolidayUtil` 只收录 **2001–2026**（且 2001 只有 2 条记录，是残缺年；2002 起才是整年）。所以 `HOLIDAY_DATA_LAST_YEAR = 2026`，**超出的年份一律不标「休 / 班」，不按"周末即休息"猜**——猜错会让律师把调休上班日当成休息日。农历与节气照常显示（可推算）。有一条测试会在上游扩到 2027 时失败，提醒同步常量。
- 数据缺口要有**出口**：月视图在含超出年份时给整个网格一个 `title` 说明，使用说明里也有一段讲清"哪些是可推算的、哪些是公告数据"。**不猜、不静默**。
- `lunar-javascript` 是 CJS 且**不带类型声明**，因此自建 `packages/core/src/lunar-javascript.d.ts` 只声明用到的那几个方法。实测两个坑：`getMonthInChinese()` **闰月时自带闰字**（返回「闰六」，不要再拼前缀）；`Solar.fromYmd(2026, 2, 30)` **不报错、静默顺延到别的一天**，所以 `almanacFor` 自己用 `Date` 往返校验日期键。月号是负数表示闰月（`-6` = 闰六月），适配层拆成"正整数月号 + `isLeapMonth`"。

**代码分工（client 包）**：`view.ts` = 纯逻辑（日期运算、区间归属、`monthWeekCount`、`shiftMonth`、`fullDateLabel`、四象限排序与容量裁剪，全部可单测）；`views.tsx` = 三个线索视图 + 使用说明 + 数据目录表单；`client.tsx` = 状态编排、首行菜单与 shell 几何；`shell.ts` = 会话区几何；`styles.ts` = 样式令牌与固定尺寸常量。

**插件设置**（2026-09-11 起）：顶部「插件设置」里有两块，**各自独立保存**（改颜色不必先设数据目录，反之亦然）：**数据目录**（`dataRoot`）与**四象限颜色**（`priorityColors`）。两者都写入 `$DSH_HOME/settings.yaml` 的 `dslegal:` 段并立即生效；`cordis.patch.yml` 里的 `config.dataRoot` 是可选的**组合层默认值**（用户设置优先）。未设定数据目录时工具与 `/dslegal/projects` 等接口返回「尚未设定数据目录」提示，`/dslegal/overview` 返回空集 + `configured: false`（界面据此改显表单）——但**颜色无论如何都下发**，否则界面连空态都画不出正确的配色。

子页标题就是**「插件设置」四个字**，不缀「· 数据目录」——这个页面现在装了两块内容，标题里只提其中一块会让人以为另一块不在。顶部仍只有「插件设置」+「返回」两个元素。

**四象限颜色的三条约定**：

1. **颜色随 `/dslegal/overview` 一起下发**，client 不再自己读设置。client 与 host 是两个插件，各算一套"用户设置 → 默认色"的兜底迟早会算出两个配色的界面。
   **缺字段时降级，不抛错**（与 `schedules`/`todos` 的处理**刻意不同**）：那里字段缺失等于"数据是错的"（会把"接口没这个字段"渲染成"今天没有安排"），而颜色缺失有一个**可证明正确的兜底**——内置默认色正是旧 host 一直在用的那一套。所以 `getOverview` 退回默认色并置 `colorsUnavailable`，界面照常可用 + 用横幅说明"重启 DSH 后自定义颜色才生效"。抛错会让整个面板在"页面已刷新、DSH 还没重启"的窗口期里直接不可用，代价远大于收益。同理，`saveColors` 在 `colorsUnavailable` 时连请求都不发——旧 host 的 POST 只认 `dataRoot`，会回一句「缺少 dataRoot。」，那个提示对用户毫无意义。
   **降级路径本身也要防崩**：`openSetup` 读的是 `/dslegal/settings`，而**旧 host 的这个接口同样没有颜色字段**——第一版直接信它，`colors[优先级]` 索引 `undefined`，React 整棵子树抛错、面板连 `aside` 都不剩（实测踩过，是拿 live 的旧 host 跑出来的）。现在草稿先落到**总览里那一份**（`getOverview` 已保证有值），设置接口的字段只在"确实有"时才覆盖；`ColorSection` 内部也一律 `?? 默认值`。
2. **解析只有一份实现**：`@dslegal/core` 的 `resolvePriorityColors()`，host 与 client 共用。它有三条性质——**逐键兜底**（某一键写坏只影响它自己，其余照常生效）、**输出恒定完整**（四个优先级一定有值，渲染方不必判空）、**问题如实报出**（`issues` 带原值与原因，不静默丢弃）。
3. **写坏的色值不落盘**：保存时先逐键兜底再写入，坏值只在那一次响应里作为 `colorIssues` 出现。另外设置层的**合并没有"删除键"这一步**，所以「恢复默认」是**显式写回四个默认色**，而不是删键——语义等价且能可靠往返。

`/dslegal/settings` 的 POST 因此是**部分保存**：`{dataRoot?}` 与 `{priorityColors?}` 任一或同时给；两个都不给返回 400。注意处理顺序必须是**先落盘、后读回**（`buildSettings` 读的是当前生效值，顺序反了会把旧值回给界面——实测踩过）。

**profile 安装方式**（本机限制：无全局 pnpm，且 `dsh plugin` 需要它）：

- 因包间用 `workspace:*`，profile 内 pnpm 无法解析 → 采用**复制构建产物**到 `~/.dsh/profiles/web/node_modules/@dslegal/{core,host,client-ui}`（package.json + lib/）。
- **产物必须自足**，因为这里是"复制"不是"安装"：`core/tsdown.config.ts` 里对 `lunar-javascript` 设了 `noExternal`，把它内联进 `lib/index.js`（35KB → 482KB）。否则 profile 里还得额外放一份 `node_modules/lunar-javascript`，漏了这一步的后果是 host 插件 import 期直接 `ERR_MODULE_NOT_FOUND`、插件整体加载失败、面板消失。**改构建配置后要重新核对**：`core/lib/index.js` 不应有任何 `import ... from "<第三方包>"`；`client/lib/client.js` 的 `require()` 只应出现 `react` 与 `react/jsx-runtime`。当前体量：core 482KB（gzip 123KB）、host 51KB、client 659KB（gzip 176KB）。
- 在 `~/.dsh/profiles/web/cordis.patch.yml` 追加 `dslegal-host` 与 `dslegal-client`（`name: '@dslegal/client-ui'`）两行（`dslegal-host` 的 `config.dataRoot` 可选）；**撤销 = 恢复该文件为 `[]` 并重启 dsh**。
- 演示数据：`sandbox/法律工作/诉讼案件/张三诉李四民间借贷/0. 协作/1. 工作日志.md`。
- 排障提示：重启"不生效"时先查端口占用（`Get-NetTCPConnection -LocalPort 3080 -State Listen`）——旧 dsh 进程残留会以 `EADDRINUSE` 顶掉新进程，浏览器看到的仍是旧实例。

**client 侧硬约束**（P4 实查所得，第三条为 2026-09-09 事故教训，第四、五条为界面布局实查所得，第六至八条为 P6 重构实查所得，第九至十四条为 P7 视觉重构实查所得）：

- 浏览器产物必须是 `window.__ModuleLoader__.load({ id, factory: (require) => {...} })` 工厂形态（tsdown `format: cjs` + `banner`/`footer`）。
- 产物只能 `require` shell 提供的共享模块（react / react/jsx-runtime / cordis / dsh-client-*）；`@dslegal/core` 必须内联（`noExternal`）。
- **包名不得以 `/client` 结尾**（故更名 `@dslegal/client` → `@dslegal/client-ui`）：DSH `dsh-client-modules` 的 `stripClientSuffix` 会把 `@dslegal/client` 误削成 `@dslegal`，boot graph 行永远查不到，浏览器报 `cannot resolve "@dslegal/client" — not a row in the boot graph`，插件装配整体失败且服务器端各探针（HTML / bundle / API）全部正常，极具迷惑性。**防复发守卫**：`packages/client/test/package-name.test.ts` 断言包名不以 `/client`（或 `client`）结尾，且 banner 的 `id` 等于包名。
- **插槽会再包一层 `display: contents` 的 div**：从插件根元素找 `shell.overlay` 层必须用 `closest('[data-shell-overlay]')`，**不能用 `parentElement`**（那是包装层，其 `grid-template-columns` 为 `none`，会静默退化成兜底值——实测踩过）。会话区几何 = AppFrame 三列 grid 的轨道宽度，用 `getComputedStyle(frame).gridTemplateColumns` 读（浏览器返回已解析 px，如 `280px 976px 0px`），见 `packages/client/src/shell.ts`；工作台面板据此铺满会话区（`left` = 侧边栏宽、`right` = 详情栏宽）。**启动按钮常驻**、不做关闭按钮：点击即开合（`aria-expanded` 反映状态），**按住可以拖动**（落点存 `localStorage`；开合仍走 `onClick`，所以键盘 Enter/空格可用，拖动收尾的那次 `click` 被吞掉），默认位置锚定到侧边栏底部设置按钮（右边缘对齐、部分重叠），**拖回原处即恢复默认、重新跟随侧边栏**；**点左侧会话菜单栏任意位置也收起面板**（侧边栏是别的插件的 DOM、面板又不覆盖它，故在 document **捕获阶段**监听 `pointerdown` 做命中判断，见 `sidebarColumnOf`）。**防复发守卫**：`packages/client/test/shell.test.ts`。
- **重测不能用 rAF 合并**：`requestAnimationFrame` 在**被遮挡/后台的标签页里不触发**，把测量放进 rAF 会让几何永久停在旧值（实测踩过）。改法是同步测量 + 三条触发源：`MutationObserver` 盯 frame 的行内 `style`（侧边栏拖动/详情栏开合都改这一行，且它是微任务，后台标签页也触发）、`ResizeObserver` 盯 frame 与三列（视口变化引起的轨道重排）、`resize` / `visibilitychange` 兜底。`useShellGeometry`（面板几何）与 `useBoxHeight`（四象限"能放几行"）都遵守这一条。
- **四个等面积象限靠网格、不靠内容**：`grid-template-rows/columns: repeat(2, minmax(0,1fr))` 撑出四个恒等区域，象限内容区必须 `flex:1 + minHeight:0 + overflow:hidden`。**少了 `minHeight:0`，条目会把容器顶高**，"能容纳几条"就随内容自激（越显示越长、越长越能显示）。容量 = `floor(内容区高度 / 固定行高)`（行高 26px，见 `styles.ts` 的 `TODO_ROW_HEIGHT`）。**防复发守卫**：`packages/client/test/layout.test.ts`。
- **标题省略号**：`overflow:hidden + textOverflow:ellipsis + whiteSpace:nowrap`，且外层必须 `minWidth:0`——flex 子项默认 `min-width:auto`，会被长标题顶住不缩，省略号永远不出现（实测：`scrollWidth 468 > clientWidth 311` 才算真的生效）。
- **client 必须校验接口形状**：client 按请求从磁盘读、刷新页面即生效，host 要重启 DSH 才换新。新 client + 旧 host 的组合下，界面会把"字段不存在"渲染成"这一天没有安排"，看起来一切正常。故 `getOverview` 校验 `schedules`/`todos` 是否存在，缺失即抛出可执行提示。**防复发守卫**：`packages/client/test/api.test.ts`。
- **显式读取强制重扫磁盘**：目录的新建/改名/删除**不触发文件监听**（监听的是已知工作日志路径，不是整棵树）。界面刷新、列出项目、定位写入目标都走 `deps.scan(true)`；只有同一轮里反复调用的 agent 工具复用缓存。**防复发守卫**：`packages/host/test/plugin.test.ts` 的「新建的案件目录无需重启即可被读到」——注意该用例必须先 `sleep` 等 `apply` 的首轮异步扫描落定并预填缓存，否则会假绿（实测踩过）。
  **已知偶发（机器忙时）**：`plugin.test.ts` 的 e2e 会以 chokidar 的 `EPERM realpath %TEMP%\dslegal-e2e-*` 报**一条 unhandled rejection**——48 条用例全绿、退出码却是 1。根因是"监听器还在走临时目录、测试已经把它删了"的竞态，与本插件逻辑无关：实测同时跑着沙箱 dsh + 无头浏览器时必现，停掉它们复跑即过。**别据此去找业务 bug。**
- **盖在面板上的全屏层必须写不透明底色**：`T.canvas` 是 `rgba(0,0,0,.022)`——拿它当「使用说明」的底，背后整片日程条会透上来，看起来像渲染错乱。全屏层一律用 `T.bgLayer1`（实测踩过）。
- **实心条上的字色按对比度算，不按亮度拍阈值**：`onColor()` 把"白字对比度"和"深字对比度"都算出来取大的那个。拍阈值（如"亮度 > 0.42 就换深色"）会让 `#ED8936`（亮度 0.362）留住白字，实测只有 2.55:1，读不了。**防复发守卫**：`packages/client/test/layout.test.ts` 的「四个优先级色都取对比度更高的那一侧」。
- **未设优先级的条目用 `T.unset`（中性石板灰），不得回退到主色**：主色在这套界面里专表"今天"，一条普通日程染成主色既是错误的信息也是错误的语义。
- **`<button>` 有 UA 默认字号（13.33px）**：自定义按钮上只写 `fontFamily: inherit` 不够，字号必须显式写死，否则会从 UA 样式表漏进来。
- **`<button>` 还有 UA 默认边框 `border: 2px outset`，不写全就会漏出来**。这是"UA 默认值从样式表漏进内联样式"的同一类坑，但后果更刺眼：实测渲染成 **2px 的 `#a8a8a8` 中灰**。踩过的具体形态是月历格子——只想画右、下两条 1px 格线，于是只写了 `borderRight` / `borderBottom`，结果上、左留下 UA 那 2px，整片月历看起来"每个日期都被描了黑框"，用户直接反馈"很丑"。**规则：凡是用在 `<button>` 上的样式，`border` 简写或四条 longhand 必须写全**；只声明一部分时，剩下那几条不是"没有边框"，而是"交给 UA 决定"。**防复发守卫**：`packages/client/test/layout.test.ts` 的「用在 `<button>` 上的样式必须把边框说全」——它从两个 `.tsx` 里**现扫** `<button ... style={UI.x}>` 得到名单（新按钮自动纳入），函数式样式用一张显式样参表取值。**断言要写 `'none'` 而不是 `undefined`**：`undefined` 恰好是"交给 UA"，第一版守卫就是栽在这里，bug 在的时候照样绿。
- **面板里没有全局 `box-sizing: border-box` 重置**（DSH 的样式表管不到我们的内联样式），所以 CSS 默认的 `content-box` 生效：写了 `height: 30` + 上下 padding 8px 的元素，**外高是 46px 而不是 30px**。实测踩过——`topBar` 写 `minHeight: 50` + `padding: 8px 16px` + 1px 下边框，量出来 **67px**，比它替代的旧头栏（53px）还高，而单看每个数字都对，很难一眼找出原因。**凡是"写死高度"又要"上下 padding / 边框"的样式都必须自己声明 `boxSizing: 'border-box'`**。注意 `minHeight: 0` 是 flex/grid 子项"不设下限"的惯用写法，不算尺寸声明，不必也不该给它加 border-box。**防复发守卫**：`packages/client/test/layout.test.ts` 的「盒模型」两条——第一条会枚举所有导出样式，失败时直接列出缺 `border-box` 的名字。
- **周形态的"共 N 条"要数**出现次数**而不是去重条数**：跨日事项在覆盖的每一天各出现一次，去重后的数字会和屏幕上能数出来的条数对不上。同理，月历格子放不下时要写「+N」，不能静默丢弃。
- **固定高度的行里不能放会换行的文本**。实测踩过：日卡的行写死 56px，元信息一折成两行正好等于内容区高度——"差 1px 就顶穿"，而折行与否取决于字体回退的度量，不取决于我们写了什么。修法是两头都堵：行高改由内容自然决定（每行都是"一个标题行 + 一个元信息行"，各行仍然等高），元信息改用 `itemMetaOneLine`（`nowrap + ellipsis`）。`itemMeta`（会换行）只许用在高度自然增长的容器里。**防复发守卫**：`packages/client/test/layout.test.ts` 的「固定高度的行不得包含会换行的文本」。
- **主体区必须自成层叠上下文**（`body()` 的 `position: relative` + `zIndex: 0`）。实测 bug：日视图下点「使用说明」，说明层盖不住日视图——日卡用 `zIndex: 8–10` 叠前后关系，而主体区没有 `position`/`z-index`，不构成层叠上下文，那些卡片就直接参与**面板**那一层的比较，`zIndex: 10` 压过了说明层的 `zIndex: 2`。修法是隔离主体区：线索内部无论用多大的 z-index 都越不出去，说明层只要 z-index > 0 就恒在其上。**防复发守卫**：`packages/client/test/layout.test.ts` 的「主体区隔离成一个层叠上下文」。

**启动按钮（2026-09-12）**：默认位置不动（右缘对齐侧边栏底部的设置按钮、底边对齐），但**可以按住拖动**，且**侧边栏折叠时也一定看得见**。

- **落点算法在 `shell.ts` 的 `launcherSpot()` / `defaultLauncherSpot()`**（纯函数，有单测）；样式层只负责画——"贴在哪儿"是几何问题，不该跟外观混在一起。
- **折叠后必须看得见**：DSH 折叠把侧边栏从 `280px` 压成 `56px` 导轨（实查），会话区左侧只剩几十像素。旧实现此时退化成"贴设置按钮右侧"，整个人落进面板里；两者 z-index 又同为 60、`aside` 在 DOM 里更靠后，**按钮被面板盖住、看不见也点不到**（实测 `elementFromPoint` 命中 `aside`）。现在：带子里放得下就贴带子右缘，放不下就贴视口左缘，由 `LAUNCHER_Z` 保证浮在面板之上。**展开态实测 `left=209`，与原实现逐像素一致。**
- **拖动**：Pointer Events + `setPointerCapture`；位移超过 4px 才算拖动；**开合仍走 `onClick`**（所以键盘 Enter / 空格照常可用），拖动收尾的那次 `click` 由 `swallowClickRef` 吞掉——不吞的话"拖完松手"会顺手开合一次面板。落点存 `localStorage`，窗口变小后会被**夹回可视区**。
- **拖回原处即恢复默认**（`nearLauncherSpot()`，20px 阈值）：不必再造「复位」按钮，也顺着"拖回去 = 回到原位"的直觉。
- **z-index 三者成套**：`BACKDROP_Z=55 < PANEL_Z=60 < LAUNCHER_Z=61`，写成常量放在一起。**防复发守卫**：`shell.test.ts` 的「启动按钮恒在面板之上」。端到端：`sandbox/probe-launcher.mjs`。

**一条流程教训：测试会把 bug 固化成"预期"**。上面那个"折叠后钻进面板"的位置，`shell.test.ts` 里**原本就有一条用例断言它是对的**（`expect(style.left).toBe(46 + 12)`，用例名还写着"不越出视口"）——作者（我）当时只想到了"别跑出视口"，没想到"别跑进面板"，于是把错误行为写成了通过的标准。**守卫测试要断言的是"用户能看到什么"，不是"我算出来的数字是多少"**：同一条 bug 用 `elementFromPoint` 做命中测试就一眼可见。凡"位置/可见性"类的断言，尽量落到"在那个点上能点到谁"或"渲染成几个像素"，而不是复述实现里的算式。

**周 / 月形态的点击粒度（2026-09-12）**：**点"日程"这一条，弹它的明细窗；改状态在窗里做。**

用户原话："月视图里点击特定日程安排时不要跳转到日视图……要准确识别用户点击的具体哪一个『日程』，并弹出悬浮窗显示对应日程的具体信息，如果没有点击到具体的某一个『日程』，则不予反馈"；以及"增加一个点击改变其『完成』与『未完成』状态的按钮，并且这种悬浮窗要复用在周视图。"

- **条目本身可点**：月历小条与周条都是 `<button>`，上报"哪一条 + 它在屏幕上的位置"（`project` + `line` 是精确定位，不是"某一天"）。**两个形态走同一个手势**。周条早先是"点整条直接切换状态"，现已改为弹窗——同一动作在两个形态里必须是同一个手势，且直接切换没有"看清楚再改"的机会。**防复发守卫**：`layout.test.ts` 的「点击粒度：周 / 月都走"点条目 → 弹明细"」（扫源码钉住 `WeekBar` 用 `onOpenSchedule`）。
- **格子是 `<div>`**：可点，但点下去是"在那格空白上新建"（见「点空白处新建」），不是按钮语义；写成 `<button>` 还会把 UA 那圈 2px 边框带回来。两个动作靠条目上的 `data-fl-item` 标记分开。
- **明细窗**（`SchedulePopover`）：优先级色点 + 标题 + `日期 / 时间 / 地点 / 项目 / 备注`（**只列有值的字段**）+ 底部**一个切换完成状态的按钮**（`data-fl="popover-toggle"`，沿用条上的 `barMark`）。内容是 `view.ts` 的 `scheduleDetail()` 纯函数产出的。
- **窗里存"身份"不存对象**：`detail` 只记 `{project, line, anchor}`，每次渲染从最新 `overview.schedules` 按身份取回。**抱着旧 `row` 会让切换按钮白点**——`toggle` 之后 `refresh()`，旧对象里的 `done` 永远是点击前的值。
- **挂在面板 `aside` 下**（格子里 `overflow: hidden`，挂进去会被裁）。落点是 `view.ts` 的 `popoverPosition()`（纯函数）：**贴条下方左对齐（实测间距 6px）→ 下方放不下翻到上方 → 左右越界夹回来**，并保证整个落在**主体区**内。单测扫过边界值。
- **关闭**：点别处（document 捕获阶段 `pointerdown` + `closest('[data-fl="schedule-popover"]')`）、`Esc`、窗口尺寸变化；换线索 / 形态 / 进设置页也关掉。**不做跟随重定位**（跟随要持续观测，收益不值）。切换按钮在窗内部，所以点它不会被"点别处"误关。
- **端到端实测**：`sandbox/probe-toggle.mjs`（20 条断言，含**回读 `/dslegal/overview` 确认状态真的落进文件**、以及**点周条本身不再改状态**）。它会改数据，live 带 `readonly`。

**一条流程教训：视觉模型说"盖住了"，几何说没盖住。** 这轮它在月历截图上断言"弹窗盖住了它指向的那条日程"，与落点算法（贴条下方 6px）矛盾。加一条几何断言实测：`overlaps=false gap=6`，**两个形态都一样**——模型错了。它读文字与判断"丑不丑"可用，**说"谁盖住谁""在不在某个位置"一律要实测**（`elementFromPoint` / `getBoundingClientRect`）。

**一个必须记住的 React 死循环陷阱（本轮实测踩过）**：悬浮窗的定位写在**没有依赖数组**的 `useLayoutEffect` 里（内容一变就得重量），而 `popoverPosition()` 每次都返回**新对象**。直接 `setSpot(next)` 就是"渲染 → 测量 → 写状态 → 再渲染"的自激，React 抛 `Maximum update depth exceeded`（错误码 #185），DSH 记一条 `slot entry crashed in 'shell.overlay'`，**整块面板连 `aside` 一起消失**——现象和"点一下面板就没了"完全一样，而服务器端一切正常。修法是**先 compare 再写回**（`view.ts` 的 `sameSpot()`，有单测）。同一道防线在 `useBoxSize` 与 `useShellGeometry` 里各防过一次：**凡"测量后写回 state"的地方都要过这一道。**

**项目详情改版（2026-09-12）**：待办**按优先级铺成 2×2 四象限网格**，并修掉"日程安排浮在界面上、盖住「不重要不紧急」那一组待办"。

用户原话："点击具体的项目后，待办事项没有按照四象限的排版规则显示，日程安排也浮在了界面中，和'不重要不紧急'的待办事项重叠了。"这是**两个**独立问题，必须分开修：

- **排版**：`detailQuadrantGrid`（`repeat(2, minmax(0, 1fr))` + `alignItems: 'stretch'`），**同行两格等高**。
- **压扁（"浮在界面上"的真因）**：`card()` 里的 `minHeight: 0` 本意是"卡片在定高布局里可被压缩"，但它**同时取消了 flex 子项默认的 `min-height: auto` 保护**；而项目详情的容器是 `listColumn()`（`overflow-y: auto` 的 flex 列）。两条合起来：**卡片被压到容器高度、内容溢出卡片底部，后面那张卡片紧接着压在上面**。修法 `detailCard()` = `card()` + `flex: '0 0 auto'`，溢出交给容器滚动。

**与「待办线索」的四象限刻意不同，两条规则别互相靠拢**：

| | 待办线索（`quadrantGrid`） | 项目详情（`detailQuadrantGrid`） |
| --- | --- | --- |
| 高度 | `1fr` 铺满可视区，四个**等面积** | **按内容**长，不写 `gridTemplateRows` |
| 内容区 | `flex:1 + minHeight:0 + overflow:hidden`，放不下就**裁剪** | 不滚动、不裁剪，`overflow` 一个都不写 |
| 溢出 | 不存在溢出（裁掉了） | 由 `[data-fl="project-detail"]` **整页滚动** |
| 为什么 | 职责是"扫一眼该做什么"，版面恒定比穷尽重要 | 职责是"把这件事从头到尾读一遍"，**少显示一条就是漏一条待办** |

- **四格恒定铺满，空格也画**并写「暂无」：藏掉空格会让 2×2 缺一角，像渲染失败；而"这一类一件事都没有"本身就是要读到的信息。**所以不许再写 `buckets.filter(total > 0)`**。
- **「未设优先级」不是第五个象限**，不进网格，单独占一整块跟在网格下面——硬塞进「不重要不紧急」是对数据的曲解。
- `groupHead` 已删除，现在只有 `detailQuadrantHead(color, hovered)`（底部一条**优先级色线**；悬停换底色，因为整条也是"在这个案件里新建一条该优先级待办"的热区）。颜色由调用方从 `overview.priorityColors` 取，**样式层不兜底**。
- **防复发守卫**：`layout.test.ts` 两组——「项目详情：四象限网格」（列 `minmax(0, 1fr)`、不写 `gridTemplateRows`、内容区不许有 `overflow`/`minHeight`、不许 `buckets.filter`、未设优先级块必须在网格之后）与「项目详情的区块不许被压扁」（`detailCard().flex === '0 0 auto'`，并**现扫 `views.tsx` 把 `data-fl-card=` 与 `UI.detailCard()` 的个数对齐**）。
- **端到端实测**：`sandbox/probe-project.mjs`（1440×900 / 1440×560、sandbox / live 各一遍）：格数 / 等宽 / 等高 / 同一水平线 / 格间重叠 / 内容溢出 / 行跑到格外 / **逐格命中测试** / **色线 vs 接口下发色**（live 四格色线与 `priorityColors` 逐一对上且互异）。

**一个探针自身的坑**：`probe-project.mjs` 里读容器滚动用的是 `S(wrap, ['overflow-y'])['overflow-y']`，早先写成 `.overflowY`（camelCase）——`getComputedStyle` 的键是 kebab 的，于是那一行**恒为 `undefined`**，报告里看着像"容器不滚动"。**探针的输出也是要验证的输出**，别拿它的字面值当事实。

**点空白处新建待办 / 日程（2026-09-12）**：**点一块空白 = 在那儿新建一条**，"点在哪儿"只决定默认值。

用户原话："当用户点击日程安排和待办事项中的空白区域（包括顶部空白位置，比如「重要且紧急」几个字这一行，又比如「09/12 今天」这个区域）的时候，弹出悬浮窗口，用于用户输入信息创建新的日程或者待办事项。"

- **热区 = 容器的空白，含各容器的标题行**（用户点名的两个例子都是标题行）：待办象限的标题条与内容区、日卡表头（`09/12 今天`）与内容区、周列的列头与泳道、月历格子、项目详情两张卡片的标题行与内容区。标题行右侧一个淡 `＋`（`blankAdd`），悬停变主色——"指着才知道能点"等于没告诉用户。
- **点在哪儿 → 预填什么**（`view.ts` 的 `composeDraftOf()`）：象限 → 该优先级；日卡/周列/月格 → 那一天；项目详情 → 那个案件；其余 → 未设优先级 + 今天。
- **两条硬规则**（`views.tsx` 的 `blankClick()`）：① 落在条目上的不算点空白——所有条目元素带 `data-fl-item`，一次 `closest` 判掉；② **必须 `stopPropagation`**——热区嵌套（项目详情里象限格嵌在卡片里），不挡冒泡会触发两次 `onCompose`，外层那次把内层预填的优先级冲掉。
- **校验只有一份实现**：`composeError()` 直接调 `@dslegal/core` 的 `formatTodoLine` / `formatScheduleLine`（host 落盘前用的同一对函数，client 已内联 core），所以"界面说没问题、写下去被打回来"结构上不可能发生。守卫禁止这里出现自己写的正则。
- **新建窗**（`CreatePopover`）：落点用鼠标位置（零高度矩形），宽度写死，挂在 `aside` 下（格子里有 `overflow: hidden`）。字段按类别给。
- **标题框自动聚焦的坑**：窗量到尺寸前挂着 `visibility: hidden`，而**隐藏元素接不了焦点**（`focus()` 静默失败，界面正常、只是光标没落进去）。`autoFocus` 同样不行（React 在提交阶段就 focus）。正解：同一个 layout effect 里以 `spot !== null` 为条件聚焦一次（`focusedRef` 保证只做一次，否则每次 hover 重渲染都把焦点从别的框拽回标题）。
- **"关窗的那一次点击"不许顺手开新窗**：关闭方式就是点别处，不处理的话点一下空白会"关掉明细窗、同时弹新建窗"。做法是文档级 `pointerdown` 在确实关了东西时立一次性标记（`swallowBlankRef`），`openCompose` 开头读它并复位；标记每次按下先复位，只对它自己那一次交互有效。
- **两个浮层统一收口**（`popoverOpenRef`，监听只装一次）：`Esc` / 点窗外关掉两者；**窗口尺寸变化只关明细窗**——新建窗锚的是鼠标点过的点，落点会被重新夹进可视区，为"锚点略偏"丢掉用户敲了一半的字不划算。
- **提交失败窗不关**：报错显示在窗里。成功才关窗 + 横幅报"已新增…（项目）"。
- **端到端实测**：`sandbox/probe-create.mjs`（sandbox 全跑、live 带 `readonly`），最有分量的三条：**回读 `/dslegal/overview` 确认真的落盘**、**点条目不弹新建窗**、**点月历小条弹的是明细窗而不是新建窗**。

**一条推翻了旧结论的改动，要留痕**：`probe-popover.mjs` 的 B 节原先断言"点空白处什么都不发生"——那是更早一轮用户的原话。这一轮用户明确要"点空白 → 弹新建窗"，所以 B 节改成断言新建窗，但**其中"不跳转日视图""焦点日期不动"两条原样保留**（用户当初最在意的是那半句）。`probe-create.mjs` 与 `probe-popover.mjs` 现在一起覆盖这条边界。

**项目选择器与"能选到哪些项目"（2026-09-12，用户反馈后）**：新建窗里的项目**永远可改**。

用户原话："当前新建日程的时候，默认绑定的项目是某一个案件，但实际上这是不一定的，应当允许用户灵活选择当前存在的真实项目，比如下拉备选，又或者输入部分关键字后列出命中的项目。"

- **症状与原因不是一回事**。"只有一个项目、而且是写死的那个"只是症状；**真正的原因是列表里本来就只有一个**：插件把路径写死成 `0. 协作/1. 工作日志.md`，而用户的数据根目录里有二十来个案件文件夹，只有 1 个符合这条路径（那个还是本项目开发时为验证造的）。其余**真实案件**的工作日志用的是另一套目录名与文件名（`0. 工作/0. 工作日志.md`、隐藏目录 `.0. 数据/…`、`1. 信息数据/…` 这类），还有的章节写成 `## 1. 待办任务`。**先查磁盘再动手**，否则会把"列表是空的"当"选择器不好用"来修。
- ⚠ **这一节刻意不写任何真实案名与目录清单**：本仓库是**公开**的（2026-09-12 起仍是 public），测试样例一律用合成案名（某某公司 / 王五 / 赵六）。写这条注释是因为曾经违反过：真实案名被当成"测试样例"写进了 `view.test.ts` 与若干注释，后来连同 `ref/` 一起清掉并重建了历史。
- **用户 2026-09-12 明确选择：两处都保持数据契约不变**——① 仍只认 `0. 协作/1. 工作日志.md`（"按文件名放宽识别""把 `.0. 数据` 这类隐藏目录也扫进来""接受 `## 1. 待办任务` 同义词"三条**都被否掉了**）；② 仍只认 `## 1. 待办事项`。**别自作主张去"修"这个不一致**：不猜路径正是为了不把条目写进用户没想到的文件里。代价（只有已就绪的项目能选）已在使用说明里如实写明。
- **选择器**：触发器（`compose-project-trigger`，显示当前项目 + 顶级目录 + 翻转箭头）→ 候选面板（关键字框 + 列表）。筛选是 `view.ts` 的 `filterProjects()`（空关键字 → 全部；前后空白忽略；项目名与顶级目录名都参与匹配）。**关键字只筛不改**：`draft.project` 只在点中候选（`pick()`）时才变——写入型界面的硬要求，有守卫盯着。
- **"只有一个项目"时也照样给这个控件**。早先是"一个项目就渲染成静态文本 + 项目详情里点出来的窗把项目锁死（`projectLocked`）"，用户看到的就是"写死了一个案子、改不了"。现在只有一条规则：**默认值不是锁**；`projectLocked` 已从代码里删除，守卫禁止它回来。
- **窗要自己限高**：候选展开后表单变高，矮窗口下会把「创建」顶出主体区。`composePopover(maxHeight)` 按 `bounds` 高度截住，中段 `composeFields` 滚动，标题与页脚恒定可见；候选列表自己限高 140px 并滚动。
- **Esc / 回车在筛选框里的语义**：`Esc` 只收起候选列表（`stopPropagation` 挡住文档级监听），再按一次才关整张窗；回车是"选这一项"、同样挡冒泡——不挡的话选完项目会顺手把窗提交掉。
- **端到端实测**：`probe-create.mjs` 第五节（R1–R15）。最有分量的是 **R11「选谁就写进谁」**（展开 → 关键字 → 点中另一个项目 → 提交 → **回读 overview 确认落在新项目里**）与 R8「筛选不改写入目标」。live 只有 1 个项目、这一节 SKIP，但 R1–R6 照样证明"一个项目时控件也在、点得开、看得见当前选的是谁"——那正是用户反馈的场景。

**验证链路（P7 建立，我无法看图，故必须靠实测）**：视觉验收走 `sandbox/cdp.mjs`——headless Edge + CDP（Node 24 自带 `fetch`/`WebSocket`，不必引 playwright），把面板裁出来逐线索拍照，**同时把 `getComputedStyle` 的几何与颜色 dump 成 `audit.json`**。几何与配色**一律以 audit 为准**，视觉模型只用来回答"丑不丑"：实测中它对"第几列什么颜色""某个元素在不在 DOM 里"这类事实并不可靠（曾断言象限色块缺失、待办行有超长路径标签、月历行高不均，三次都是臆造）。专用探针：

- `sandbox/probe-fonts.mjs` —— 遍历面板里每个有文本的元素统计实际字号档位（证明"字号统一了"）。
- `sandbox/probe-stacking.mjs` —— 用 `document.elementFromPoint` 做**命中测试**。它返回该点上真正在最上面的元素，是判断"谁盖住谁"最直接的办法；并且会临时抹掉可疑属性再测一次做 A/B，把"相关"变成"因果"。
- `sandbox/probe-month.mjs` + `sandbox/report-month.mjs` —— 月视图专项。前者除了 dump 几何与逐格农历（`data-fl-mark` / `data-fl-almanac` / `data-fl-num`），还会**截一条横跨格缝的 7×1 像素带并解 PNG 读回 RGB**——"线有几像素、什么颜色"这类问题只有像素能回答，`getComputedStyle` 只能告诉你"声明了 1px"。
- `sandbox/probe-monthhead.mjs` —— 量日期行三格的 x 区间，证明 `[日号] [休/班] ……… [农历]` 真的是"左—中—右"，靠右那条真的贴右。
- `sandbox/probe-popover.mjs` —— 点击行为专项，见「周 / 月形态的点击粒度」。**B 节自 2026-09-12 起断言"点月格空白 → 弹新建窗"**（旧行为已被推翻），但同节保留"不跳转日视图""焦点日期不动"两条。
- `sandbox/probe-create.mjs` —— 「点空白 → 新建」专项，见该节。**会改数据**（新建之后再删掉，按标题前缀 `探针新建` 清理），只对沙箱跑；live 带第三个参数 `readonly`（不提交、不碰文件、只验弹窗与预填）。用法 `node probe-create.mjs <baseUrl> <outFile> [readonly]`。踩过的两个探针坑都写在里面：**选热区要写 `> div:first-child`**（象限/卡片里有两个 `> div`，不加限定会点到内容区）、**项目详情要先 `scrollIntoView`**（live 有 16 条待办，下面的卡片在视口外，`elementFromPoint` 返回 null，报告里只看到莫名其妙的 FAIL）。清理必须**一次删一条并重新回读**：删掉一条后它下面的行号整体上移，拿旧行号删下一条会被 host 的 stale 守卫拒绝，报告却写着"删掉了 N 条"。
- `sandbox/probe-toggle.mjs` —— 明细窗里那个「完成 / 未完成」按钮 + 周形态复用，见同一节。**会改数据**，带 `readonly` 跑 live。
- `sandbox/probe-launcher.mjs` —— 启动按钮专项：折叠侧边栏 + 拖动，见「启动按钮」。
- `sandbox/probe-project.mjs` —— 项目详情专项：区块卡片有没有被压扁、四象限网格是不是真的 2×2（等宽 / 同行等高 / 互不重叠 / 内容不溢出 / 行没跑到格外）、**逐格命中测试**（把该格最后一行滚进视口，看那个点上最上层是不是它自己那一格）、以及**四格色线 vs `/dslegal/overview` 下发的 `priorityColors`**（证明接线接到了用户设置上，而不只是"参数被用出去了"）。用法 `node probe-project.mjs <baseUrl> <outFile> [w] [h] [shotName]`，**只读、不改数据，可以直接跑 live**。
- `sandbox/probe-overflow.mjs` —— 八个界面状态的整体体检（日程 · 日 / 周 / 月、待办、项目列表、项目详情、插件设置、使用说明），每个状态报四项：**内容溢出容器（`overflow` 不是 auto/scroll 却装不下）**、**兄弟元素重叠（排除浮层）**、**文字被截断且没有省略号**、**嵌套滚动容器**，末尾附页面报错。用法 `node probe-overflow.mjs <baseUrl> <outFile> [project] [w] [h]`（第 4 个参数写 `project` 才会点进项目详情）。它是**发现"插件设置底部按钮点不到"和"卡片被压扁"的那把扫帚**——改完布局先跑它，1440×900 与 1440×560 各一遍。
- `sandbox/probe-sidebar.mjs` —— 探查 DSH 侧边栏本身（按钮清单、折叠控件的 `aria-label`、折叠前后的轨道宽度），并回答"按钮中心点上最上层是谁"。**要碰别人插件的 DOM，先跑它把事实摸清楚**（折叠控件实查是 `button[aria-label="收起侧边栏"]`）。

**探针必须收集页面报错**：面板崩掉时 DOM 只是"少了一块"，光看断言只会看到一串 FAIL 而不知道原因。`probe-popover.mjs` 的 `Cdp` 类订阅 `Runtime.exceptionThrown` 与 `Runtime.consoleAPICalled`，最后一段打印页面报错（**这轮就是靠它一眼认出 React #185**）。注意 `Runtime.enable` 会**回放上一轮遗留页面的 console**，所以要测的页面加载完成、进入目标状态之后**清空一次**，否则尸检结果会混进本轮报告。

**报告要过一遍编码这一关**：`audit.json` 里的中文经 PowerShell 管道会被按 GBK 解码，`ConvertFrom-Json` 直接报错、`Get-Content` 输出成乱码（**文件本身是好的**）。所以让探针顺便写一份**纯 ASCII 摘要**（如 `report-month.txt`：日期键、休/班、颜色、像素 hex 全用 ASCII），用 `read` 工具读它，别用 PowerShell 读中文 JSON。

**像素解码不用装依赖**：PNG 是 zlib + 逐行滤波器，`node:zlib` 的 `inflateSync` 加约 40 行反滤波就能解 CDP 截图，不必为了读几个像素引 sharp/pngjs。

**流程教训**：浏览器侧产物**必须在真实浏览器验证后才能装入 profile**——服务器端 200 不能替代浏览器端验证；未验证的产物装进运行中的 profile 可能拖垮整个 harness。

**已确定的关键选择**：

- 首批实现类别：**民事诉讼**（其余 7 类暂不推进）。
- 项目类别来源：**工作日志的 H1 标题**（`# 工作日志_民事` 等），不新增元数据文件。
- 提醒：**暂不纳入**，先实现基础功能。
- 日历视图：**自建**（date-fns + CSS Grid）。**不引第三方日历 UI 库**这条仍然成立；但**农历 / 节气 / 法定节假日改用 `lunar-javascript` 推算**（2026-09-12 起）——历法算错比界面难看严重得多，不自己手搓。
- 工具命名：一律加 `legal_` 前缀。

---

## 9. 文档维护规则

- **改仓库里的文本文件，只用文件工具（read / write / edit），禁止用 PowerShell 的 `Set-Content` / `Out-File` / `-replace | Set-Content` 回写。**
  这条已经踩过**四次**（改 http.ts、改 AGENTS.md、改 styles.ts 做 A/B、改 client.tsx 改类型名），每次症状一样：`Get-Content` 用 GBK 读进来、`Set-Content -Encoding UTF8` 写回去，于是**整份中文被重新编码成乱码（不可逆，`?` 已经丢字节）并加上 BOM**，代码行看着没变、注释全废，伴随几条毫不相关的测试变红。**做 A/B 也要用 edit 工具改回去**，不要图省事用 PowerShell。需要批量核对时，让 Node 脚本去读写（Node 按 UTF-8 处理，没有这个问题）。
- **本仓库是公开的（GitHub public）**：真实案名、当事人姓名、案件目录结构、界面截图、本机绝对路径与个人信息，一律不许进代码、注释、测试样例、文档与**提交信息**；测试数据只用合成案名（某某公司 / 王五 / 赵六）。`ref/` 已进 `.gitignore` 并从磁盘删除；2026-09-12 曾因此**删库重建、把历史压成一个提交**——推上去的东西收不回来，`git rm` 也抹不掉旧对象。
- 需求 / 技术决策变更 → 更新《需求分析与技术栈设计》。
- 数据格式变更 → 更新《数据约定规范》（**三方契约，改动需谨慎并同步告知用户**）。
- 新增非代码文档 → 放入 `maintenance/`，文件名以**递增序号**开头（如 `3. xxx.md`），并在本文件第 2 节登记。
- 保持文档与代码同步；实现偏离文档时，**先改文档再改代码**（或至少同步修改）。
